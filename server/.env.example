PORT=3040
NODE_ENV=development

# Без PostgreSQL: раскомментируйте (файл server/data/dev.sqlite)
DATABASE_DEV=sqlite

# PostgreSQL (как user-service в next) — не используйте вместе с DATABASE_DEV=sqlite
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mnozh_db
DB_USER=mnozh_user
DB_PASSWORD=mnozh_password
